const CloudinaryWidget = () => {
  const widget = window.cloudinary.createUploadWidget({ cloudName: "dnmpjwixe", uploadPreset: "tsdo8id4" }, (error, result) => { })
}

export default CloudinaryWidget
